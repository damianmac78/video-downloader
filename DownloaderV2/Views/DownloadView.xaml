<UserControl x:Class="DownloaderV2.Views.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <TextBlock Text="BOLT FORGE MEDIA MANAGER" Style="{StaticResource SectionHeader}" Margin="1,0,0,9" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" ToolTip="Paste a video URL" Margin="0,0,8,0" />
                    <Button Grid.Column="1" Content="Analyse" Command="{Binding AnalyseCommand}" Style="{StaticResource PrimaryButton}" MinWidth="92" />
                </Grid>
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,12,0,0" MinHeight="130">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <Border Background="#0E1215" CornerRadius="4" ClipToBounds="True">
                        <Grid>
                            <TextBlock Text="NO ARTWORK" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" FontSize="11" />
                            <Image Source="{Binding Video.ThumbnailUrl}" Stretch="UniformToFill" />
                        </Grid>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,2,0,2" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Video.Title, TargetNullValue=Analyse a video to see its details}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="48" />
                        <TextBlock Text="{Binding Video.Uploader, TargetNullValue=Uploader}" Foreground="{StaticResource MutedTextBrush}" Margin="0,9,0,0" />
                        <TextBlock Text="{Binding Video.DurationText, TargetNullValue=Duration}" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="65" /><ColumnDefinition Width="155" /><ColumnDefinition Width="18" />
                        <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Quality" Foreground="{StaticResource MutedTextBrush}" />
                    <ComboBox Grid.Column="1" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}" IsEnabled="{Binding IsNotBusy}" />
                    <TextBlock Grid.Column="3" Text="{Binding OutputFolder}" TextTrimming="CharacterEllipsis" ToolTip="{Binding OutputFolder}" />
                    <Button Grid.Column="4" Content="Browse…" Command="{Binding BrowseCommand}" Margin="10,0,0,0" />
                </Grid>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,10,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <Button Content="Download" Command="{Binding DownloadCommand}" Style="{StaticResource PrimaryButton}" MinWidth="110" />
                        <Button Grid.Column="1" Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource DangerButton}" Margin="8,0,0,0"
                                Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Grid.Column="3" Text="{Binding PercentageText}" FontWeight="SemiBold" />
                    </Grid>
                    <ProgressBar Value="{Binding ProgressPercentage, Mode=OneWay}" Maximum="100" Height="8" Margin="0,12,0,8"
                                 Foreground="{StaticResource AccentBrush}" Background="#0E1215" BorderThickness="0" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="90" />
                            <ColumnDefinition Width="Auto" /><ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding StatusText}" TextTrimming="CharacterEllipsis" ToolTip="{Binding StatusText}" />
                        <TextBlock Grid.Column="1" Text="Speed" Foreground="{StaticResource MutedTextBrush}" Margin="14,0,7,0" />
                        <TextBlock Grid.Column="2" Text="{Binding Speed}" />
                        <TextBlock Grid.Column="3" Text="ETA" Foreground="{StaticResource MutedTextBrush}" Margin="14,0,7,0" />
                        <TextBlock Grid.Column="4" Text="{Binding Eta}" />
                    </Grid>
                    <Expander Header="Technical details" Margin="0,9,0,0" Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBox Text="{Binding ErrorDetails, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" MaxHeight="90"
                                 VerticalScrollBarVisibility="Auto" Margin="0,7,0,0" />
                    </Expander>
                </StackPanel>
            </Border>

            <Grid Grid.Row="4" Margin="0,10,0,0">
                <Button Content="Open Output Folder" Command="{Binding OpenFolderCommand}" HorizontalAlignment="Left" />
                <TextBlock Text="Audio-only downloads are added to the Music Library" Foreground="{StaticResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Right" />
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
